{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}
{% block body %}
    {{ form_start(form) }}
    <h1 text-align:center position:top>Ajouter un client</h1>
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
{% endblock %}
